Nenrin（和暦年齢メモ）

-------------------------------
要件・機能

共通
　・アプリ名：和暦年齢メモ
　・AppStore Bundle ID: com.azukid.agememo
　・Apple ID: 6805710017
　・横向きにも対応
　・iPhone/iPad/Macに対応
　・iPhoneSE3の幅に収まるようにする（縮小または横スクロール）
　・Targetは最小に、17かな？
　・DivigoやPacklinの理念やコード構成を参考にしたい

主画面
　・西暦＝和暦＝干支（漢字、絵、かな）＝年齢（誕生日以降の満年齢）　の一覧
    縦スクロール（インジケータ不要）
    初期は当年を中央に表示
    範囲は江戸から2100年まで、負荷や無駄がないか要検討
　・各年にメモ（MAX400文字）できる
    メモがあれば２行目に小さく表示
    JSON保存で十分だろう
　・行タップでメモ全文とカレンダーを表示
　・上部にAdMobバナー広告を表示
　・ヘッダー部に　設定ボタン、タイトル、移動ボタン　を配置
    　・移動：西暦/年号と年を指定して移動（移動専用テンキーシート）
        （昭和100年や令和-20年なども可とする）

設定画面
　・初心者/達人モード設定
　・文字サイズ設定
　・ダークモード設定
　・生年月日を設定すれば、「自分」から生年を中央に表示できる

下部タブバー
　・年齢：年齢を指定して年齢→生年一覧の該当年に移動（年齢専用テンキーシート）
　・自分：自分の年→年齢一覧を表示、現在年を中央に表示（生年月日が未設定ならば設定シートを開く）
　・名簿：名簿一覧、追加、編集、一覧から選択してその人の年→年齢一覧を表示する

-------------------------------
詳細設計

> **改訂記録**
> 上の「要件・機能」は最初に書いた入力そのままで、以降は変更していない。
> 実装を経て確定した仕様との差分は以下のとおり。詳細は各節に記す。
>
> | 項目 | 当初案 | 確定 |
> |---|---|---|
> | 下部バー | 6ボタン（現在/自分/年齢/干支/移動/設定） | 3タブ（年齢/自分/名簿）＋ナビバー（設定/移動）— §5 |
> | 「現在」 | 独立ボタン | 「年齢」タブへ統合 — §4.2 |
> | 干支シート | 複数選択して該当年を一覧 | **実装しない**（設定の干支表示で足りる）— §5 |
> | 名簿 | 要件のみ、設計なし | `PersonStore` として設計 — §3.3 / §5.2 |
> | 補助表示 | なし | 賀寿・厄年・学齢・九星・六曜・月齢を追加 — §2.4 |
> | 年齢の数え方 | 満年齢のみ | 満年齢／数え年を選択可 — §6 |
> | 生年月日入力 | `DatePicker` | テンキー式の自前入力 — §6 |
> | カレンダー | 12ヶ月グリッド or 月送り | 月送り式1ヶ月。大安・満月を色分け — §4.5 |
> | 一覧の列 | 収まらなければ行全体を縮小 | 基本3列は縮小せず、補助列は常に縦積みの固定幅 — §4.4 |
> | 多言語 | 「en を足すなら元号をローマ字」 | **暦のデータは ja のまま、操作の案内だけ en 対応** — §13 |
> | ラテン名 | `AgeMemo` | `Nenrin` — §13 |


## 1. 方針

Divigo（DialSplit）の構成を基本形とする。SwiftData は使わず、メモは JSON 1ファイルに保存する。
Packlin からは「初心者/達人モード」「文字サイズ」「AdMob バナー」の考え方を引用する。

| 項目 | 決定 |
|---|---|
| アプリ名 | 和暦年齢メモ（ラテンブランド名 `Nenrin`） |
| Bundle ID | com.azukid.agememo |
| Apple ID | 6805710017 |
| 最小Target | iOS 17.0（Divigo と同じ。SwiftUI の `ScrollPosition` 系は 17 で足りる） |
| 対応 | iPhone / iPad / Mac (Designed for iPad) |
| 向き | Portrait / Landscape 両対応 |
| 言語 | ja / en。暦のデータは ja のまま、操作の案内だけ訳す（§13 参照） |
| 永続化 | UserDefaults（設定）＋ JSON ファイル（メモ） |
| 依存 | GoogleMobileAds のみ。AZDial/AZCalc は不要。Firebase は当初案にあったが導入しない |

### ディレクトリ構成

```text
AgeMemo/
├── App/                 — AppMain、AppSettings、Config
├── Core/                — JapaneseEra、Zodiac、AgeCalculator、
│                          Longevity、UnluckyYear、SchoolAge、NineStar、Rokuyo、MoonPhase
├── Model/               — YearRow、MemoStore、Person
├── Components/          — AZPicker、NumericKeypad、BirthDateInput、
│                          BeginnerHelpBanner、AppAppearance、FittedSheetHeight、SheetCloseButton
├── Features/
│   ├── Year/            — 主画面（一覧、行、詳細、カレンダー、下部タブ）
│   ├── Jump/            — 年齢／移動／名簿の各シート
│   ├── Settings/        — 設定画面
│   └── Ads/             — AdMobBanner
└── Resources/           — Assets、Info.plist、PrivacyInfo.xcprivacy
```


## 2. ドメイン（Core）

### 2.1 和暦 JapaneseEra

Foundation の `Calendar(identifier: .japanese)` が江戸期の元号を保持していることを実機確認済み。
自前の元号テーブルは持たず、Foundation に委ねる。

- 1600年→慶長5年、1700年→元禄13年、1800年→寛政12年、1868年→慶応4年/明治元年、2100年→令和82年
- 対象範囲 1600〜2100 の中に**改元年が40件**ある（慶長20→元和元 …… 平成31→令和元）

改元年は1年の中に2つの元号が同居するため、行データは元号を**配列**で持つ。

```swift
/// 1年に含まれる和暦区間（改元年は2件以上になる）
struct EraSpan {
    let eraName: String     // 例 "令和"
    let eraYear: Int        // 例 1（元年）
    let isGanNen: Bool      // eraYear == 1
    let startMonth: Int     // その年の中で有効になる開始月
    let startDay: Int       // その年の中で有効になる開始日
}
```

- 表示は `元年` 優先（`平成元年`）。2年目以降は `平成2年`
- 改元年は `慶応4年 / 明治元年` のようにスラッシュ区切りで併記する
- 元号名・元号年は `DateFormatter(dateFormat:"Gy")` の結果を parse するのではなく、
  `Calendar.japanese` の `.era` / `.year` コンポーネントから直接取得する（ロケール非依存で安全）

**構築コスト対策**：1600〜2100 の501件は起動時に一度だけ計算して `[YearRow]` としてキャッシュする。
501件 × 各12ヶ月の日付生成は避け、**1/1 と 12/31 の2点だけ**を見て元号が変わるかを判定し、
変わる年のみ二分探索で改元月日を特定する（501×2＋40×log(365) 程度。実測で数ミリ秒）。
特定した改元月日は `EraSpan.startMonth` / `startDay` に保持し、詳細カレンダーの区切り線と元号ラベルに使う。

### 2.2 干支 Zodiac

干支は西暦の剰余で決まるため計算のみ。テーブル不要。

```swift
let branchIndex = ((year - 4) % 12 + 12) % 12   // 子丑寅卯辰巳午未申酉戌亥
let stemIndex   = ((year - 4) % 10 + 10) % 10   // 甲乙丙丁戊己庚辛壬癸
```

検算：1600=庚子、1868=戊辰、1926=丙寅、2026=丙午、2100=庚申。実装済みテストで確認する。

表示は要件どおり3態を持つ。

| 要素 | 例（2026年） |
|---|---|
| 漢字 | 丙午（十干十二支）／午（十二支のみ） |
| かな | ひのえうま ／ うま |
| 絵 | 🐴（Assets に SF Symbols 非依存の自前12点、または絵文字） |

一覧行では**十二支の絵＋漢字**、詳細シートで**十干十二支＋かな**まで出す。

### 2.3 年齢 AgeCalculator

年齢列は「現在」と「自分」で2種類の基準を切り替える。

```swift
currentAge  = currentYear - rowYear  // その年生まれの人の当年時点
personalAge = rowYear - birthYear    // 自分がその年の誕生日を迎えた後
```

- **現在**：生年月日は使わず、各行の年に生まれた人の当年時点の年齢を常に表示する
- **自分**：設定した生年月日を基準に、各行の年における誕生日以降の満年齢を表示する
- 「自分」で生年月日が未設定のときだけ年齢列を出さない（`age` は `Int?`）
- 「年齢」「干支」「移動」「設定」の操作では、直前に選んだ「現在」または「自分」の年齢基準を維持する

**マイナス年齢**：基準年より後の生年、または生年より前の行は負値として表示する。

| 年 | 生年1988の場合 | 表示 |
|---|---|---|
| 1986 | -2 | `-2歳` |
| 1988 | 0 | `0歳`（生年） |
| 2026 | 38 | `38歳` |

「現在」で当年が2026年の場合は、2026年が `0歳`、2027年が `-1歳`、2025年が `1歳` となる。

- 「生まれる何年前か」を読む値なので、`-2歳` とそのまま出す（`2年前` とは書かない）
- 負値は色を落として（`.secondary`）正の年齢と視覚的に区別する
- 下限は 1600 年なので、生年によっては -400 台まで出る。桁あふれしないよう年齢列の幅は4桁分確保する
- 年齢列の幅は「最大絶対値の桁数＋符号」で決め、生年月日の変更時に再計算する


### 2.4 補助表示の暦計算

一覧と詳細に「その年が何の年か」を添えるための計算群。いずれも設定でON/OFFする。
表は持たず計算で求めるものと、短い定数表を持つものに分かれる。

| モジュール | 求めるもの | 方式 |
|---|---|---|
| `Longevity` | 還暦・古希・喜寿などの賀寿 | 13件の定数表（数え年）を引く |
| `UnluckyYear` | 前厄・本厄・後厄、大厄 | 男女別の本厄表（数え年）＋前後1年 |
| `SchoolAge` | 小1〜小6・中1〜中3・高1〜高3・大1〜大4 | 入学年からの経過年数で判定 |
| `NineStar` | 九星気学の本命星 | 各桁を1桁になるまで足し、11から引く |
| `Rokuyo` | 大安・仏滅などの六曜 | 旧暦の `(月 + 日) % 6` |
| `MoonPhase` | 月齢と月相 | 旧暦の日を朔からの日数とみなす概算 |

**数え年との対応**：賀寿と厄年は本来数え年で数えるため、表示中の年齢が満年齢のときは
数え年へ直してから判定する。設定の「年齢の数え方」を変えると判定位置もずれる。

**立春区切り**：九星は元日ではなく立春で切り替わる。立春は年により2月3〜5日に揺れるが、
暦の公表値を持たないため通例の2月4日で判定する。1月〜立春前日生まれは前年の星になる。

**旧暦**：六曜と月齢は `Calendar(identifier: .chinese)` を日本時間で使う。
六曜が月替わりで飛ぶのは算出式どおりの挙動で、不具合ではない。

**厄年に性別が要る**：本厄の年齢が男女で異なるため、性別が未指定なら厄年は表示しない。
`Gender` は `Person` にも持たせ、名簿の人物でも同じ判定を行う。


## 3. データモデル

### 3.1 YearRow（表示用・非永続）

```swift
struct YearRow: Identifiable {
    let id: Int             // 西暦年 = そのまま識別子
    let gregorian: Int
    let eraSpans: [EraSpan]
    let stemBranch: StemBranch
}
```

`YearRow` には西暦年から決まる不変データだけを持たせる。
年齢は `AppSettings.birthDate` と `gregorian` から表示時に導出し、生年月日未設定時のみ `nil` とする。
メモは `MemoStore` を正とし、`gregorian` をキーに表示時に参照する。
これにより、生年月日の変更とメモの保存を `YearRow` の再構築なしで即時反映する。

### 3.2 メモの永続化 MemoStore

JSON 1ファイル。件数は多くても数百のため全読み・全書きで十分。

保存先：`Application Support/AgeMemo/memos.json`（`.excludedFromBackup` は付けない＝iCloudバックアップ対象）

```json
{
  "version": 1,
  "memos": {
    "1989": { "text": "平成に改元", "updatedAt": "2026-08-27T10:00:00Z" },
    "2026": { "text": "…", "updatedAt": "…" }
  }
}
```

- キーは西暦年の文字列
- 1件 MAX 400文字（`APP_MAX_MEMO_LEN = 400`）
- 空文字になったらキーごと削除する
- 書き込みは自動保存の実行時のみ。`Data.write(to:options:.atomic)` で原子的に置換
- 起動時に読み込み `[Int: Memo]` として `@Observable` な `MemoStore` が保持する
- `MemoStore` の更新は主画面と詳細シートで共有し、保存・削除後の表示を即時更新する

将来の拡張余地として `version` を持たせるが、第一版では移行処理を書かない。

### 3.3 名簿の永続化 PersonStore

メモと同じ流儀で JSON 1ファイル。保存先は `Application Support/AgeMemo/people.json`。

```swift
struct Person: Identifiable, Codable, Equatable, Sendable {
    let id: UUID
    var name: String
    var birthDate: Date
    var gender: Gender      // 厄年の判定に使う
}
```

- 配列の順序をそのまま並び順とする（利用者が長押しドラッグで決める）
- 追加は常に末尾へ。生年月日で自動ソートはしない
- `gender` は後から追加したフィールドのため、`decodeIfPresent` で既存データも読めるようにする
- `MemoStore` と同じく `@MainActor @Observable` として保持する


## 4. 主画面（Features/Year）

### 4.1 レイアウト

```text
┌──────────────────────────────┐
│ [⚙設定]    タイトル    [↗移動] │  ← ナビゲーションバー
├──────────────────────────────┤
│  （初心者モードのみ）ヒント     │
│  [AdMob バナー]                │  ← 上部固定
├──────────────────────────────┤
│ 2024  令和6年   🐲 辰   36歳  │
│ 2025  令和7年   🐍 巳   37歳  │
│ 2026  令和8年   🐴 午   38歳  │  ← 初期は当年が中央
│       ↳ メモ本文の1行目…       │  ← メモがあれば2行目に小さく
│ 2027  令和9年   🐑 未   39歳  │
├──────────────────────────────┤
│   [年齢]    [自分]    [名簿]   │  ← 下部タブ（3つ）
└──────────────────────────────┘
```

- 縦スクロール、`.scrollIndicators(.hidden)`
- 横向き・iPad では列間の余白を広げる。列の並びは変えない
- 行タップで詳細シート

### 4.2 スクロール実装と負荷（要件の「負荷や無駄がないか要検討」への回答）

**結論：501行の `LazyVStack` で問題ない。仮想化や分割ロードは不要。**

- 501行は `List`/`LazyVStack` の遅延生成で画面内 20行程度しか実体化されない
- 和暦・干支は起動時計算済みの構造体を引くだけ
- 年齢は表示時に `gregorian - birthYear` で導出する軽量な計算
- メモは `[Int: Memo]` の辞書引き O(1)

`List` ではなく `ScrollViewReader` + `ScrollView` + `LazyVStack` を採る。理由は、`List` の区切り線・インセット制御より、
`scrollTo(_:anchor:)` による任意年への移動が素直に書けるため。

```swift
ScrollViewReader { proxy in
    ScrollView {
        LazyVStack(spacing: 0) {
            ForEach(rows) { row in
                YearRowView(row: row)
                    .id(row.id)
            }
        }
    }
}
.scrollIndicators(.hidden)
```

初期表示・各操作の移動は `proxy.scrollTo(year, anchor: .center)` で行う。
各年の行コンテナに西暦年の `id` を明示し、初回レイアウト後に移動する。
`anchor: .center` により要件の「中央に表示」を満たす。

起動時は「年齢」タブを既定の選択状態とし、生年月日の設定状態にかかわらず当年を中央に表示する。
（当初案の「現在」ボタンは「年齢」タブに統合した。生年月日を必要としない基準はこれ1つで足りる）

### 4.3 範囲

`1600...2100`（501年）。要件の「江戸から」は江戸開府1603年だが、
慶長年間の切りの良さと干支の一巡を考え **1600年始まり**とする。

### 4.4 行の表示

**すべての行で列位置と文字サイズを揃える。** 行ごとに列がずれたり大きさが変わったりすると、
一覧として目で追えなくなる。この一点を優先して以下を決めた。

#### 列構成

最大5列。左から順に並べ、設定でOFFの列は場所ごと詰める。

| | 列 | 幅 |
|---|---|---|
| 1〜3 | **基本3列**（西暦・和暦・年齢） | 内容に応じた固定幅 |
| 4 | 干支／九星 | 固定（下記） |
| 5 | 学齢・賀寿・厄年 | 固定（下記） |

- 基本3列の並びは「西暦・和暦・年齢」。
  ただし**「年齢」タブのときだけ年齢を左端**に置く（`showsAgeFirst`）。
  年齢から生年をたどる向きの操作なので、目線の始点に年齢がある方が引きやすい
- 改元年は和暦欄を `慶応4年` / `明治元年` のように元号ごとに改行し、2行で表示する
- 改元年の和暦欄以外は改行しない

#### 文字サイズ

- **基本3列は設定した文字サイズのまま出す。縮小しない。**
  行ごとに大きさが変わると読みにくいため、狭いときに縮むのは補助列だけとする
- 補助列の倍率は `ViewThatFits` が 100%〜55% の6段階から選ぶ

#### 補助列は常に縦積み・固定幅

干支と九星、学齢・賀寿・厄年は、それぞれ**常に縦に積む**。横並びと縦積みが
混在すると列の高さが揃わないため、文字サイズや該当数では切り替えない。

幅は想定される最長の語で固定し、該当しない年でも場所を確保する。
`fixedSize` で中身に合わせると行ごとに幅が変わり、列が揃わなくなる。

| 列 | 基準 | 幅 |
|---|---|---|
| 干支＋九星 | 一白水星（4文字） | `九星の文字サイズ × 4` |
| 干支のみ | 絵文字＋漢字1文字 | `文字サイズ × 2.6` |
| 学齢・賀寿・厄年 | **大還暦**（3文字） | `補助の文字サイズ × 3` |

学齢は「小1」、厄年は「前厄」でいずれも2文字だが、同じ列に入る賀寿に
「大還暦」があるため3文字ぶんを確保する。

#### 余白

列間と左右両端はすべて可変の `Spacer` とし、余った幅を均等に配る。
補助列が固定幅なので、3列なら4か所、5列なら6か所へ同じ幅で分かれる。
最小値は列間 `baseColumnSpacing`（10pt）、左右端 `edgeInset`（12pt）。
- 当年は生年行と同じ濃さのアクセント色で塗り、ダークモードでも視認できるようにする
- 生年は当年と同じ濃さのアクセント色で塗る
- 生年より前の行はマイナス年齢（`-2歳`）を `.secondary` で表示する
- 移動シートから着地した行は緑系の選択色で示し、生年行の青系と区別する
- メモがある行は2行目に `lineLimit(1)` の `.caption` で本文先頭を出す
  （「メモは自分だけに表示」がONのときは「自分」タブでのみ出す）

### 4.5 詳細シート

行タップで開く。
- 上部：西暦／和暦（全元号）／年齢／干支（十干十二支＋かな＋絵）
- 補助表示の設定にかかわらず、該当する干支・九星・長寿祝い・厄年・入学卒業を表示する
- 見出しの改元年は元号を並べず、年初の元号だけを置き、`令和元年（5月1日〜）` を次行に添える
- 中部：メモ編集（`TextEditor`、400文字）
- 下部：**その年のカレンダー**（月送り式1ヶ月）
  - 改元年はカレンダー上で改元日に区切り線と元号ラベルを出す
  - 各日に六曜、月替わりで月齢を添える
  - 月の移動は前後ボタン・左右スワイプ・月のプルダウン選択の3通り
  - **大安は赤系、満月（輝面比100%）は黄色系**で示す。日取りを選ぶときに
    真っ先に探される2つなので、他の日と同じ `.secondary` では埋もれる。
    色だけに頼らず太字も併用する
- 保存は自動（`.onChange` から500ミリ秒のデバウンス）
- 入力が続いた場合は待機中の保存タスクを取り消し、最新の本文で再予約する
- シートを閉じる時とアプリが非アクティブになる時は、待機中の変更を即時保存する
- `MemoStore` 内で書き込みを直列化し、古い保存結果が新しい本文を上書きしないようにする
- 書き込み失敗時は未保存状態を保持し、次の自動保存時に再試行する

**カレンダーの実装方針**：`Calendar.gregorian` から月初の曜日と日数を出して7列グリッドを組む自前実装。
`UICalendarView` は1600年台の表示や和暦併記の自由度で不利なため使わない。

当初案は「12ヶ月グリッド または 月送り式」の両論だったが、**月送り式1ヶ月**に決めた。
六曜を各日に入れると12ヶ月グリッドでは文字が潰れるため。

**iPad のシート高**：iOS 18 以降は `presentationSizing` で中間サイズを提案する。
既定のままだと画面いっぱいに広がって余白が目立つため、メモ欄の有無で高さを変える
（メモあり 1040pt / なし 820pt、幅は 720pt）。


## 5. 下部タブとナビゲーションバー

**当初案は6ボタン等幅の自前ツールバーだったが、実装では「年齢基準の切替（3タブ）」と
「その場の操作（ナビゲーションバー）」に役割を分けた。**

性質の異なるものが1列に並んでいたのが理由で、前者は*状態*、後者は*操作*である。
状態を選ぶものだけを下部タブに残し、選択中のタブがアクセント色になる素直な作りにした。

### 下部タブ（年齢基準の切替）

3ボタン等幅。`AgeDisplayMode` を切り替え、当年を中央へ移動する。

| タブ | 動作 |
|---|---|
| 年齢 | `.age` — 「その年に生まれた人の当年時点」を表示。生年月日は不要。**年齢列を左端に置く** |
| 自分 | `.personal` — 自分の生年月日を基準に各年の年齢を表示。**未設定なら設定シートを開く** |
| 名簿 | `.person(Date)` — 名簿シートを開き、選んだ人の生年月日を基準にした一覧へ切り替える |

### ナビゲーションバー（その場の操作）

| ボタン | 位置 | 動作 |
|---|---|---|
| 設定 | 左（`gearshape`） | 設定シートを開く |
| 移動 | 右（`arrow.up.forward`） | 年齢／西暦／元号年を指定して移動するシートを開く |

**主画面は常に全件（1600〜2100）を表示する。** どの操作も主画面の行集合を変えない。

### 干支シートは実装しない

当初案の §5.2「干支シート」（十二支を複数選択して該当年をシート内に一覧表示）は**見送った**。

設定の「干支」をONにすれば一覧の各行に干支が出るため、12年周期を眺める用途は主画面で足りる。
専用シートを足すと「主画面に作用しないシート」という例外的な存在が1つ増え、
下部タブが状態の切替であるという整理も崩れる。必要になった時点で改めて検討する。

### 5.1 年齢シート

- テンキー（0-9、⌫、決定）。Divigo の `NumpadView` を引用
- 生年月日未設定なら先に設定シートを促す
- 決定で `birthYear + age` へ移動

**符号ボタン（-/+）**：マイナス年齢に対応するため、テンキーに符号切替を置く。

```text
┌─────────────────────────┐
│        -12歳            │  ← 符号込みで表示
│  7  │  8  │  9          │
│  4  │  5  │  6          │
│  1  │  2  │  3          │
│  ⌫  │  0  │  -/+        │  ← 「00」を「-/+」に差し替え
│        決 定             │
└─────────────────────────┘
```

- Divigo の `NumpadView` は最下段が `⌫ / 0 / 00` だが、本シートでは `00` を **`-/+`** に置き換える
- `-/+` は入力桁に触れず**符号だけ**を反転する。0 のときは符号を付けない（`-0` を作らない）
- プレースホルダー表示中に `-/+` を押した場合は、初期値の符号を反転して確定入力に移す
- 入力範囲は **-150〜150**（絶対値150まで）
- 移動先 `birthYear + age` が 1600〜2100 の外なら端に丸め、その旨を注記する（移動シートと同じ扱い）
- 生年月日が未設定なら符号ボタンも含めて操作させない（先に設定シートへ誘導）

### 5.2 名簿シート

家族・親戚・友人など、年齢を調べたい人を登録しておく。当初案には無く、実装で追加した。

- `Person`（`id` / `name` / `birthDate` / `gender`）を JSON 1ファイルに保存する
- 名前は最大20文字（`maximumPersonNameLength`）。件数の上限は設けない
- 行タップで `AgeDisplayMode.person(birthDate)` へ切り替え、シートを閉じる
- 右上の＋で追加、行右端のボタンまたは左スワイプで変更・削除
- 削除は確認アラートを挟む。取り消しはできない
- **並べ替えは長押しドラッグ**。利用者が決めた順序を保存し、追加・変更で崩さない
  （追加は常に末尾へ置く）
- 登録件数に応じてシート高さを見積もり、一定以上は伸ばさずスクロールさせる
- `gender` は厄年の判定にのみ使う。既存データとの互換のため `decodeIfPresent` で読む

### 5.3 移動シート

要件の「昭和100年や令和-20年なども可とする」を満たすため、**実在しない元号年も換算する**。

- 左：選択（年齢／西暦／令和／平成／昭和／大正／明治／…江戸期の主要元号）
- 右：年をテンキー入力（**マイナス入力可**）
- テンキーは年齢シートと同じ `NumericKeypad`、入力表示、移動ボタンを使う
- 左端の選択には文字サイズ設定に追従する `AZDropdownPicker` を使い、「年齢」を「西暦」の前に置く
- 「年齢」は設定中の満年齢／数え年を使って当年から生年を逆算する
- 一覧が「自分」の場合、「年齢」は自分の生年月日を基準にその年齢になる西暦年へ移動する
- 一覧が名簿の人物表示の場合も、選択中の人の生年月日を基準に同様に移動する
- 入力行は左端に年号選択、中央に入力値、右端に換算後の西暦年を表示する
- 初期選択は対象範囲の最新元号とし、2026年なら「令和　8」を表示する
- 2回目以降は直前に選んだ入力種別を復元し、入力値は最初の数字で上書きできるプレースホルダーとして表示する
- 入力種別を変更した場合は移動先の西暦年を維持して換算し、西暦2000年から令和へ変更した場合は「-18」を表示する
- 「西暦」を選択した場合は右端の換算後西暦年を表示しないが、中央の入力位置は維持する
- 未入力時は当年相当の西暦年または元号年をプレースホルダー表示し、数字入力開始時に置き換える
- 数値に桁区切りコンマは表示しない
- 入力は西暦のみ4桁まで、元号年は3桁までに制限する
- 移動後は対象行を緑系の選択色にして着地したセルを明示し、生年行の青系表示と区別する
- 移動シートと年齢シートの実行ボタンは移動先行と対応する緑系で表示し、ダークモードでは眩しくならない濃い緑を使う
- 行タップ後は対象行をオレンジ系で明示し、一覧切り替えまたは移動時に解除する
- 換算式：`西暦 = 元号の元年の西暦 + 元号年 - 1`
  - 昭和元年 = 1926 → 昭和100年 = 1926 + 100 - 1 = **2025年**
  - 令和元年 = 2019 → 令和-20年 = 2019 - 20 - 1 = **1998年**
- 換算結果を実行前にプレビュー表示する（「昭和100年 → 2025年」）
- 換算結果が 1600〜2100 の外なら端に丸め、その旨を注記する

元号の元年テーブルは、Foundation から起動時に構築した `eraSpans` を逆引きして自動生成する。
江戸期の元号は数が多いため、ピッカーは**近代（明治以降）を既定で表示し、江戸期は折りたたむ**。


## 6. 設定画面

Divigo / Packlin の設定を踏襲。`AZPicker` 相当のラジオ／ドロップダウンを自前で持つ。

| 設定 | 選択肢 | 既定 |
|---|---|---|
| 表示モード | 初心者 / 達人 | 初心者 |
| 文字サイズ | 自動 / 標準 / 大 / 特大 | 自動 |
| 外観モード | 自動 / ライト / ダーク | 自動 |
| 生年月日 | テンキー式の自前入力（元号選択可） | 未設定 |
| 年齢の数え方 | 満年齢 / 数え年 | 満年齢 |
| 性別 | 未指定 / 男性 / 女性 | 未指定 |
| メモは自分だけに表示 | OFF / ON | **ON** |
| 干支 | OFF / ON | OFF |
| 九星 | OFF / ON | OFF |
| 入学・卒業 | OFF / ON | OFF |
| 長寿祝い | OFF / ON | OFF |
| 厄年 | OFF / ON | OFF |

- 名簿の名前入力は単一行とし、キーボードの完了キー、名前欄以外のタップ、スクロール、シート移動でキーボードを閉じる
- 名前入力のキーボード表示中は現在のシート高を維持し、シートを自動的に上へ拡大しない
- 生年月日が正常な通常時はエラー表示領域を確保せず、テンキー下の余白を詰める

- 生年月日は「未設定」状態を持つ（`Date?`）。クリアボタンを置く
- 生年月日未設定で「自分」を選んだ場合は設定シートから生年月日入力を直接開き、「自分の生年月日を登録してください」と案内する
- 生年月日の年号選択は文字サイズ設定に追従する `AZDropdownPicker` を使い、選択中は縦書き、候補一覧は横書きにして展開マークを表示しない
- 生年月日は `DatePicker` ではなく**テンキー式の自前入力**（`BirthDateInput`）とした。
  1600年台まで遡る入力を `DatePicker` のホイールで行うのは現実的でなく、元号での入力もできないため
- **年齢の数え方**は当初案に無かった追加。長寿祝い・厄年は本来数え年で見るものなので、
  表示中の年齢とこれらの判定基準を一致させるために設けた
- **性別**は厄年の判定にのみ使う。未指定なら厄年を表示しない（判定できないため）
- **メモは自分だけに表示**は既定ON。年齢・名簿の一覧ではメモを伏せるため、
  他人に画面を見せる場面でメモが目に入らない。既定値だけ `object(forKey:)` で有無を見てから読む
- ナビゲーションバー左の「設定」ボタンから常に開けるシートとする
- **初心者モード**：主画面上部と各シートに `BeginnerHelpBanner` で使い方を表示する
- **達人モード**：ヒントを消し、行を詰めて情報密度を上げる（`compact`）
- 文字サイズは Divigo と同じく `dynamicTypeSize()` をルートに適用（自動時のみシステム追従）
- その他：バージョン表示、取扱説明リンク、レビュー導線、開発者支援（第一版は任意）


## 7. 広告

- 主画面**上部**にアダプティブバナー（要件どおり。Divigo はフッターだが本アプリは上部指定）
- 非パーソナライズ広告固定（`npa=1`）。ATT ダイアログは出さない
- バナーの上下に十分な余白を取る（誤タップによる配信停止を避ける運用ルール）
- Info.plist に `GADApplicationIdentifier` と `SKAdNetworkItems`、`PrivacyInfo.xcprivacy` を用意
- 広告ユニットIDは DEBUG でテストID、RELEASE で本番IDに切り替える


## 8. Config.swift（主要定数）

グローバル定数ではなく `enum AppConfig` の `static let` にまとめた（名前空間を汚さないため）。

```swift
enum AppConfig {
    static let yearRange = 1600...2100              // 一覧の範囲（慶長5年〜令和82年）
    static let maximumMemoLength = 400              // メモ最大文字数
    static let maximumAgeInput = 150                // 年齢入力の絶対値上限（-150〜150）
    static let maximumPersonNameLength = 20         // 名簿の名前の最大文字数
    static let memoSaveDelayNanoseconds: UInt64 = 500_000_000  // メモ自動保存のデバウンス
}
```

行高・フォント・色は Packlin の `Config.swift` と同じ流儀で、文字サイズ別のヘルパー関数として定義する。


## 9. テスト

`AgeMemoTests` に純ロジックのユニットテストを置く。UI テストは撮影用のみ。

- 元号：1600/1700/1800/1868/1912/1926/1989/2019/2026/2100 の期待値一致
- 改元年：40件の抽出結果と改元月日が期待どおりか、元年表記になっているか
- 干支：1600=庚子、1868=戊辰、1926=丙寅、2026=丙午、2100=庚申
- 年齢：生年は0歳、生年より前は負値（1986/生年1988 = -2）、生年月日未設定のみ nil、生年月日変更の即時反映
- 年齢シート：`-/+` の符号反転、`-0` を作らないこと、範囲 -150〜150、移動先の丸め
- 名簿：追加は末尾、並べ替えの保持、名前20文字での切り詰め、`gender` 無しデータの読み込み
- 賀寿・厄年：満年齢／数え年の切替で判定位置がずれること、性別未指定なら厄年が出ないこと
- 九星：立春前後（2月3日と2月5日生まれ）で本命星が変わること
- 移動換算：昭和100年→2025、令和-20年→1998、範囲外の丸め
- メモ：400文字制限、空文字での削除、JSON 往復、デバウンス、即時保存、連続書き込みの順序保証


## 10. 実装順序

1. Xcode プロジェクト作成（`AgeMemo.xcodeproj` を正とする。XcodeGen 不使用）
2. Core（JapaneseEra / Zodiac / AgeCalculator）＋ ユニットテスト
3. Model（YearRow / MemoStore）
4. 主画面の一覧とスクロール位置制御
5. 詳細シート（カレンダー＋メモ編集）
6. 下部タブ3種とナビゲーションバーの操作、各シート
7. 設定画面
8. AdMob 組み込み
9. アイコン、fastlane、取扱説明ページ


## 11. 未決事項

- **Mac 対応**：「Designed for iPad」で出す想定。Mac Catalyst にすると GoogleMobileAds が使えないため採らない
- **メモの iCloud 同期**：第一版は端末内のみ。要望があれば `NSUbiquitousKeyValueStore`（Packlin の userId 方式）で検討
- **アイコン**：`Scripts/AppIcon-source.svg` を起点に作成済み


## 12. App Store 配信物（fastlane）

メタデータとスクリーンショットは `fastlane` で管理する。構成は体調メモ・割勘に倣う。

### メタデータ

`fastlane/metadata/{ja,en-US}/` に name / subtitle / promotional_text /
keywords / description / release_notes を置く。

- レーンは `preview_metadata`（Preview.html で確認して停止）と `upload_metadata` の2つ
- 認証は App Store Connect API キー（`.p8`）。`fastlane/.env` に鍵情報を置く
- **`.env` と `*.p8` は `.gitignore` 済み。** テンプレートの `.env.example` だけコミットする
- `Deliverfile` には全レーン共通の既定だけを書く。`skip_screenshots` などを
  ここに書くとレーン引数より優先され、重複アップロードを招く（CreditMemo での教訓）

en の description は ja の翻訳ではなく別構成にする（§13 の方針に従う）。
冒頭で「暦のデータは日本語のまま」であることを明示する。

### スクリーンショット

UITest ターゲット `NenrinUITests` から `fastlane snapshot` で撮る。3カット × 2言語 × 2機種。

| | 内容 |
|---|---|
| 01YearList | 主画面（年齢）1963年へ移動 |
| 02Personal | 1963年9月1日生まれの自分一覧で当年を表示 |
| 03YearDetail | 自分一覧の1963年詳細 |

**撮影用の状態は `SnapshotSetup`（`#if DEBUG`）が起動引数を見て用意する。**
生年月日・名簿・文字サイズを UI 操作で作ろうとすると、テンキー入力や
設定トグルの連打が必要で壊れやすい。

**アプリの起動は1回だけにする。** USB 上の Xcode + シミュレータでは2回目の
`launch()` がクローン起動になり、`RequestDenied` / `launch-failed` で必ず失敗する
（体調メモでも同じ事象）。補助表示の ON/OFF は再起動ではなくアプリ内で切り替える。

**UITest から要素を掴むための識別子**：タブに `tab.age` / `tab.personal` / `tab.person`、
設定ボタンに `nav.settings`、各行に `row.<西暦>`。
SwiftUI では行の中の Text が別要素になるため、行には
`.accessibilityElement(children: .combine)` を併用して1要素にまとめる。

`concurrent_simulators(false)` と `number_of_retries(3)` は、クローン起動が
拒否される環境への対策。`derived_data_path` の固定は使わない（体調メモで実証済み）。


## 13. 多言語対応の方針

**日本文化に属する表示は日本語のまま残し、操作の案内だけを en 対応する。**

当初案の「en を足す場合は元号をローマ字にし、干支は英名併記」は採らない。
このアプリが提供しているのは和暦・干支・六曜・厄年という**日本文化そのもの**であり、
それを英訳すると情報が失われる。「令和8年」を "Reiwa 8" と書き換えたところで、
利用者が実際に免許証や戸籍で目にするのは「令和8年」の方である。

一方で、ヘルプ・エラー・操作ボタンまで日本語のままだと、外国人は**操作に詰まる**。
表示されている内容が読めないことと、操作の仕方が分からないことは別の問題である。
そこで**中身は日本語、案内は英語**という線引きを採る。

### 何を訳し、何を訳さないか

判断基準は「**それは日本文化のデータか、アプリの操作案内か**」の一点。

| 訳さない（ja 固定） | 訳す（ja / en） |
|---|---|
| 元号名（令和・昭和） | ボタン・タブ（設定→Settings、移動→Jump） |
| 干支の漢字とかな（丙午・ひのえうま） | ヘルプ文（初心者モードの説明） |
| 賀寿の名称（還暦・米寿） | エラー（保存できませんでした） |
| 年齢の数え方（満年齢・数え年） | 設定の見出し（表示モード→Display Mode） |
| 厄年の語（前厄・本厄・大厄） | 確認ダイアログ（削除しますか？） |
| 六曜（大安・仏滅） | 設定項目の見出し（文字サイズ→Text Size） |
| 九星（一白水星） | 空状態の案内（名簿が空です） |
| 月相（三日月・寝待月） | 入力エラー（存在しない日付です） |
| 学齢（小1〜大4） | |

**単位の「年」「歳」「月」「日」は訳さない。** 数字に添える文字であり、
一覧の桁揃えと行の縮小率計算に影響するため。取説の対応表で意味を示す。

**選択肢が文化データなら、見出しを訳しても選択肢は訳さない。**
「年齢の数え方」は操作の見出しなので `Age reckoning` と訳すが、
選択肢の「満年齢」「数え年」は暦の数え方そのものなので日本語で出す。
`Kazoedoshi` とローマ字にすると、片方だけ英語になって非対称にもなる。
説明文（ヘルプ）の側で `満年齢 (man-nenrei)` と読みを添えて補う。

**賀寿・厄年・九星などは名称を残したまま、設定画面の説明文だけを英語にする。**
「還暦」が何かは en 取説の The cultural markers で解説済みであり、
アプリ側は「一覧に長寿祝いを表示する」という*操作の説明*だけ英語にすればよい。

### 想定利用者

en の読者は「日本語話者」ではなく、**日本文化に関心のある外国人**を想定する。
日本在住者、日本語学習者、家系や歴史を調べる人、日本の書類を読む必要がある人。
彼らが必要としているのは翻訳ではなく、**日本語のまま読めるようになるための解説**である。

### 方針

| 対象 | 扱い |
|---|---|
| アプリ UI | ja / en。`Localizable.xcstrings` に操作案内のみ登録する |
| 文化的な語（干支・厄年・大安・還暦） | **日本語のまま残す**。取説でローマ字読みと意味を併記 |
| 画面上のボタン（設定・移動・自分・名簿） | **en では英語**（Settings / Jump / Age / Myself / Roster） |
| 元号 | 日本語表記のまま。取説に換算表（令和＝+2018 など）を添える |
| 単位（年・歳・月・日） | 日本語のまま。桁揃えに影響するため |
| 取扱説明ページ | ja / en の2言語。**en は ja の翻訳ではなく別構成** |

### en 取扱説明ページの構成

ja 版は「機能の説明」だが、en 版は前段に**前提知識の解説**を置く。

1. **Background** — 元号の仕組み、元年の書き方、改元年に2つ並ぶ理由、数え年と満年齢の違い
2. **Reading the screen** — 画面上の日本語ラベルの対応表（設定 *settei*、移動 *idō*、歳 *sai* …）
3. **Features / How to** — 機能と操作
4. **The cultural markers** — 賀寿・厄年・六曜・九星・学齢の文化的背景
   （還暦で赤いちゃんちゃんこを着る、仏滅は式場が割引、42歳が「しに」に通じる、など）
5. **Era reference** — 元号換算表

冒頭で「**表示される暦の内容は日本語**」であることを明示する。
操作は英語になるが、一覧に並ぶ元号・干支・六曜は日本語のままだと先に伝えておく。
これを書かずに公開すると、全面英語を期待した利用者が落胆する。
逆に、日本語のまま読めるようにする解説だと最初に示せば、それは欠点ではなく前提になる。

**2. Reading the screen の対応表は、日本語のまま残す語を対象とする。**
ボタンが英語化されたため、当初の「設定 *settei*」のような操作ラベルの対応表は不要になった。
代わりに一覧・詳細に現れる**日本語のデータ**（元号・干支・賀寿・厄年・六曜・九星）の読み方を示す。

### 実装方針

- `Localizable.xcstrings`（String Catalog）を `Resources/` に置き、`knownRegions` に `en` を追加する
- **訳す文字列だけ**をカタログに載せる。文化データはコード内の日本語リテラルのままにする
  - `Zodiac` / `Longevity` / `NineStar` / `Rokuyo` / `UnluckyYear` / `SchoolAge` / `MoonPhase` の
    `name` `kana` は `String` のまま。`LocalizedStringKey` にしない
  - これらの型は `Foundation` のみに依存させ、`SwiftUI` を import しない構造を保つ
- 訳す側は `Text("...")` のキーをそのまま使い、カタログで en 値を与える
  （キーを英語 ID に振り直すと ja 側の差分が大きくなるため、日本語をキーとして使う）
- 補間を含む文字列は引数の順序が言語で変わりうるため、カタログの位置指定子を使う
- **補間の書式指定子は「包んだか」で決まる。** API（`Text` / `String(localized:)`）は関係ない。
  - `\(intValue)` — そのまま渡すと **`%lld`**
  - `\(String(intValue))` — `String()` で包むと **`%@`**

  本アプリは桁区切りを出さないために `String()` で包んでいる箇所が多く、両方が混在する。
  手書きしたキーが実際と1文字でも違うと、その訳は一生使われない（画面には ja が出続ける）。
  迷ったら `LocalizedStringKey("…")` / `String.LocalizationValue("…")` を
  `print` すれば実際の `key` が確認できる。推測で書かないこと。
  ビルド後にカタログを開き、`extractionState` が付いていないキー＝自作の死にキーがないか確認する
- **暦の用語そのものはラベルも訳さない。** 「九星」「十二支」は分類の名前であって
  操作の案内ではないため、`Text(verbatim:)` で ja のまま出す。
  代わりに**読みを言語で切り替える**：ja は `（いっぱくすいせい）`、
  en は `（いっぱくすいせい / ippaku suisei）` とローマ字を併記する。
  ローマ字は `Zodiac` / `NineStar` に `romaji` として持たせる
- 数値と単位の連結（`\(age)歳`）は訳さない対象なので、そのまま残す

**確認方法**：スキームの App Language を English にして起動し、
一覧の元号・干支が日本語のまま、ボタンとヘルプが英語になっていることを見る。


### アプリ名

- ja：和暦年齢メモ
- **en：Nenrin**（アプリ名・ナビゲーションタイトルの両方）
  - ホーム画面の名前は `InfoPlist.xcstrings` の `CFBundleDisplayName` で切り替える
  - ナビゲーションタイトルは `String(localized:)` で引く。
    名簿の人物名を出す分岐があるため `LocalizedStringKey` にはできない
- ラテンブランド名：**Nenrin**（年輪。年を1本ずつ積み重ねた記録の意）
  - Packlin / Calclin / Vitalin / Deferin と同じ `-lin` / `-in` 系の語感に揃えた
  - en 取説では由来（年輪＝木の年輪）を1文で説明する。造語に見えないようにするため
  - App Store Connect での登録可否は要確認
